/**
 * CDP helpers stub for zerotoken.
 * Provides getHeadersWithAuth for browser client providers.
 */

/**
 * Generate headers with authentication for CDP connections.
 * Simple stub - returns empty headers since we're connecting locally.
 */
export function getHeadersWithAuth(_wsUrl: string): Record<string, string> {
  return {};
}
